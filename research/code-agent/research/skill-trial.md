# Code Agent 源码深度研究 Skill 首次试用记录

试用对象：当前 ZCode 仓库。基线：`872ad960de7ec172591f7e1952f7849229f94521`。阶段：系统全景、A/B 静态专题，以及用户追加的六项源码深挖已完成。用户明确研究目标是学习设计思想，不要求启动目标程序。

| 验收项     | 本阶段结果             | 依据或限制                                                                                               |
| ---------- | ---------------------- | -------------------------------------------------------------------------------------------------------- |
| 全景先行   | 通过                   | Overview 从 UI 输入追到 Host、CLI、模型、工具和 UI 投影，不以目录树代替执行链                            |
| 深入实现   | 初版未通过用户的新粒度标准；修订待复核 | 用户指出原报告虽列触发和函数，却不足以复现 Microcompact 的实际裁剪。已把“可据此实现同等关键行为”作为新门槛，主动复核相邻 A/B 专题。 |
| 表示分离   | 通过                   | 明确区分持久化、运行时、模型输入和 UI 投影，并给出 tool error 示例                                       |
| 正确性分支 | 通过（静态）           | 覆盖 duplicate、guide/queue、stop、权限修改、超时、compact retry/中断、artifact fallback；未运行故障场景 |
| 证据可信   | 通过（静态与局部测试） | E01–E34 有基线和源码定位；一组服务层测试 5/5 通过，且明确其不覆盖 CLI 核心机制或真实 Provider            |
| 示例有用   | 初版不足；已补可复算输入 | B5 已给出触发、候选批次、33 字符整条替换和 867 token 节省；其他 A/B 专题补最小边界例子。等待用户进一步校准，均非运行记录。 |
| 系统结论   | 通过（初步）           | Overview 与 synthesis 使用同一边界，并列出不可下的结论                                                   |
| 增量续做   | 通过（文档）           | 复用已有六篇专题，按用户新范围深化并新增 B5，同步回写 index、Overview 与 synthesis                       |
| 未知处理   | 通过                   | 每个专题缺口均标注影响和建议验证方向                                                                     |

## 本次发现的 Skill 改进点

1. 大输出必须按“执行捕获 → 工具格式化 → 通用序列化 → artifact → Provider 回灌”逐层核对。后层保存内容不能补回前层已丢弃的字节。
2. compact 不能只写成一种摘要操作；需要区分完整摘要压缩、microcompact 和 Provider 处理，并核对持久化与冷恢复语义。
3. Memory 的去重、冲突和过期策略需要区分代码强制与提示词约束，不能把模型应遵守的规则写成系统 invariant。
4. 试跑状态要分别报告静态源码、测试支持和真实运行，防止“已实测”掩盖验证层级。

以上四项已反写到问题库、证据深度标准、报告模板和试用验收规则。

## 用户后续校准（2026-09-22）

Microcompact 的第一版虽有触发阈值和白名单，但读者仍无法判断“整条替换还是留 preview、对哪些批次、如何计算收益”。用户要求报告达到可复现程度，并要求主动把同一粒度扩展到相关模块，不再逐段提醒。为此，B5 加入保持源码顺序的伪代码与数字例子；A1/A2/B1/B2/B3/B4 同批复核；Skill 核心流程和深度标准加入主动相邻审查。持久读者偏好见 [研究读者与深度约定](reader-contract.md)。这些修订已做结构与静态检查，**尚未由用户确认达到预期，也未运行目标 Agent/Provider**。

## 后续研究边界

- CLI 核心机制、真实应用和 Provider 未运行；这限制可宣称的证据类型，但不影响本次源码思想研究的完成判定。
- 尚未执行源码基线变化后的增量复核；本轮为同一 commit 上的专题增量研究。
- Skill 仍是 `skill-drafts/` 中的已生成且通过结构检查版本，未安装。
